parameters:
    test: value

services:
    service: 
        class: Service
        arguments: ["@library"]
        tags:
            - { name: container.injectable }
    library:
        class: Library
    fs:
        class: Symfony\Component\Filesystem\Filesystem
